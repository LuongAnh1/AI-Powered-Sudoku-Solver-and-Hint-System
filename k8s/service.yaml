apiVersion: v1
kind: Service
metadata:
  name: sudoku-api-service
spec:
  type: NodePort   # <--- ĐẢM BẢO CÓ DÒNG NÀY!
  selector:
    app: sudoku-ai
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080