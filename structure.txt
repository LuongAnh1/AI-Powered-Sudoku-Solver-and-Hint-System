```code
AI-Powered Sudoku Solver & Hint System/
├── .github/                        # Thư mục cấu hình GitHub tự động hóa (Workflows)
│   └── workflows/                  # Thư mục chứa các tệp kịch bản CI/CD
│       ├── docker-ci.yml           # Tự động hóa đóng gói và đẩy Docker Image tối ưu nhất lên Docker Hub
│       └── package-releases.yml    # Tự động hóa biên dịch chéo đa nền tảng và xuất bản GitHub Releases (Win/Mac/Linux)
├── .gitignore                      # Cấu hình các file và thư mục bỏ qua không đẩy lên Git
├── .dockerignore                   # Cấu hình các file và thư mục không sao chép vào Docker khi xây dựng Image
├── Dockerfile                      # Cấu hình biên dịch đa giai đoạn (Multi-stage Build) tối ưu dung lượng cực nhẹ cho Docker
├── docker-compose.yml              # Cấu hình quản lý và khởi chạy nhanh Web Server tự động chỉ bằng một câu lệnh
├── CMakeLists.txt                  # Cấu hình biên dịch mã nguồn C++ (C++ Engine & Pybind11 Bridge)
├── requirements.txt                # Quản lý thư viện Python chạy ứng dụng chính (OpenCV, CustomTkinter...)
├── requirements-dev.txt            # Thư viện phục vụ R&D huấn luyện mô hình (PyTorch, ONNX, onnxscript...)
├── README.md                       # Tài liệu giới thiệu dự án & Luồng làm việc hàng ngày
├── SETUP.md                        # Tài liệu hướng dẫn cài đặt compiler & môi trường ban đầu
├── structure.txt                   # Sơ đồ cấu trúc thư mục này (Bản đồ dự án cập nhật mới nhất)
├── app/                            # THƯ MỤC CHỨA KHỐI UI & COMPUTER VISION (PYTHON)
│   ├── main.py                     # Điểm khởi chạy chính của ứng dụng Python (Giao diện đồ họa Desktop - CustomTkinter)
│   ├── api_server.py               # Máy chủ Web API (FastAPI) tích hợp sẵn giao diện Web tương tác đầy đủ tính năng giải số nháp
│   ├── run_solver.py               # Kịch bản chạy tích hợp đầu-cuối thực tế (CV + C++ Solver)
│   ├── test_pipeline.py            # Kịch bản kiểm thử quy trình xử lý ảnh và xuất 81 ảnh ô con phục vụ debug
│   ├── sudoku_solver_cpp.cp312-win_amd64.pyd # Thư viện động C++ đã liên kết tĩnh (Gọi trực tiếp từ Python 3.12 trên Windows)
│   │
│   ├── hint_manager/               # Gói điều phối trung gian (Python Controller) quản lý gợi ý (Đã mô-đun hóa)
│   │   ├── __init__.py             # Khai báo gói và xuất khẩu lớp chính SudokuHintManager
│   │   ├── constants.py            # Quản lý các hằng số màu sắc hiển thị trạng thái gợi ý
│   │   ├── gui_sync.py             # Bộ xử lý đồng bộ giao diện (đọc/ghi lưới, đếm và cập nhật số nháp)
│   │   ├── chain_generator.py      # Bộ tương tác lõi C++ (nạp ma trận, phân tích và lập lịch chuỗi giải trước)
│   │   └── manager.py              # Bộ điều khiển chính điều phối luồng logic gợi ý (Next Hint / Quick Solve)
│   │
│   ├── cv/                         # Phân vùng xử lý ảnh (Computer Vision)
│   │   ├── __init__.py
│   │   ├── detector.py             # Tiền xử lý, tìm lưới 9x9, căn góc và cắt 81 ô vuông
│   │   └── recognizer.py           # Nhận diện chữ số (OCR), lọc chữ số ứng viên (Pencil Marks)
│   │
│   ├── gui/                        # Phân vùng quản lý giao diện (UI/UX Desktop App)
│   │   ├── __init__.py
│   │   ├── sudoku_cell.py          # Định nghĩa ô Sudoku đơn lẻ (hỗ trợ hiển thị số lớn và lưới số nháp)
│   │   ├── sudoku_board.py         # Quản lý lưới 9x9 gồm 81 ô, chia thành 9 phân vùng 3x3
│   │   └── app_layout.py           # Lớp giao diện chính điều phối tổng thể (bảng số, thanh điều khiển, khung xem ảnh, khung log)
│   │
│   ├── models/                     # Thư mục lưu trữ các file Model học máy đã train
│   │   ├── digit_model.onnx        # File đồ thị kiến trúc mạng neural ONNX
│   │   └── digit_model.onnx.data   # File chứa trọng số mạng neural đã huấn luyện (tự sinh ra khi xuất ONNX)
│   │
│   └── test_images/                # Thư mục chứa các ảnh Sudoku mẫu để test thuật toán CV
│
└── src/                            # THƯ MỤC CHỨA LÕI THUẬT TOÁN (C++ ENGINE)
    ├── types.hpp                   # Định nghĩa cấu trúc dữ liệu lõi (Cell, Grid, HintResult, RegionType)
    ├── solver_engine.hpp           # Khai báo bộ chạy điều phối chính (Waterfall Orchestrator)
    ├── solver_engine.cpp           # Định nghĩa bộ chạy chính (Điều phối các thuật toán)
    ├── bindings.cpp                # File đăng ký hàm và lớp C++ xuất khẩu sang Python (Pybind11)
    ├── DEVELOPER_GUIDE.md          # Tài liệu này hướng dẫn chi tiết về kiến trúc mã nguồn của lõi thuật toán C++ (Engine)
    │
    ├── strategies/                 # Thư mục chứa các nhóm thuật toán logic (Tách module)
    │   ├── strategy_manager.hpp    # Khai báo chung các hàm thuật toán để Engine gọi (Quản lý formatter chung)
    │   ├── basic_strategies.cpp    # Cấp 1 & Cấp 2: Singles, Subsets (Pairs, Triples, Quads)
    │   ├── intersection_strategies.cpp # Cấp 2.5 & Cấp 3.5: Pointing, Box/Line Reduction
    │   ├── fish_strategies.cpp     # Cấp 3 & Cấp 4: X-Wing, Swordfish, Jellyfish (Đã tối ưu hóa đệ quy chập tổ hợp)
    │   └── wing_strategies.cpp     # Cấp 4.5: XY-Wing, XYZ-Wing
    │
    ├── utils/                      # Thư mục chứa các công cụ tiện ích bổ trợ
    │   ├── hint_formatter.hpp      # Khai báo các hàm định dạng chuỗi giải thích cho giao diện đồ họa
    │   └── hint_formatter.cpp      # Triển khai logic sinh chuỗi giải thích chi tiết, hỗ trợ đa ngôn ngữ
    │
    └── tests/                      # Thư mục chứa hệ thống kiểm thử tự động (Test Cases)
        ├── test_runner.cpp         # Hàm main() chạy kiểm thử tích hợp và kiểm tra lõi C++ độc lập
        ├── unit/                   # Thư mục chứa các bài kiểm thử đơn vị (Unit Tests)
        │   ├── test_basic.cpp      # Test riêng lẻ các thuật toán Cấp 1
        │   └── test_intersection.cpp # Test riêng lẻ các thuật toán Cấp 2
        └── integration/            # Thư mục chứa các bài kiểm thử tích hợp (Integration Tests)
            ├── test_engine.cpp     # Đọc đề bài mẫu, cho Engine giải và đối chiếu kết quả
            └── test_cases/         # Thư mục chứa các file đề bài mẫu dạng văn bản (.txt)
```
